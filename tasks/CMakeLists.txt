# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(joint_trajectory_followerTaskLib)
ADD_LIBRARY(${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_NAME} SHARED 
    ${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_SOURCES})
add_dependencies(${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${JOINT_TRAJECTORY_FOLLOWER_TASKLIB_HEADERS}
    DESTINATION include/orocos/joint_trajectory_follower)

